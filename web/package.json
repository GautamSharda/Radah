{
  "name": "radah-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix ../app run build:web",
    "start": "serve -s dist -l ${PORT:-4173}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
